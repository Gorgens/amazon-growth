---
title: "Material e Métodos"
output: html_document
---

```{r packages2}
require(tidyverse)
require(magrittr)
require(plotly)
require(reshape2)
require(ggplot2)
require(V.PhyloMaker)
require(phytools)
```
Sobre os inventários... processamento e padronização...

#```{r mergeInventarios, child="02merge.Rmd"}
#```

Remove não árvores, remove indivíduos não indentificados e remove indivíduos sem diâmetro...

```{r cleanInv}
inv.paisagens = read.csv("dados/10_invPaisagensMerged.csv")
inv.paisagens = inv.paisagens %>%
  filter(type == "O" | is.na(type)) %>%
  filter(scientific.name != 'NI') %>%
  drop_na(DBH)

head(inv.paisagens)
```

Determinar o número de parcelas em cada sítio...

```{r}
parcelasArea = inv.paisagens %>%
  group_by(area, plot, subplot, year) %>%
  summarise(obs = n()) %>%
  group_by(area, year) %>%
  summarise(nplots = n()) %>%
  group_by(area) %>%
  summarise(nplots = mean(nplots))
```

Incremento diamétrico? Crescimento relativo?


Grupo ecológico?


Espécies comerciais e não comerciais?


Modelo bayesiana...
